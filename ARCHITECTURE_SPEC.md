# 🏛️ وثيقة المواصفات الهندسية: نظام "شاهر الثاني" (Shaher II OS)
## موديول: مصنع المحتوى المتكامل (فيديو + بوسترات) والمساعد الذكي المستقل
## Project: Autonomous AI Content Orchestrator & Audience Growth Architecture

---

### 1. الرؤية والفلسفة الأساسية (The Core Mission & Retention First)
الهدف هو بناء **ماكينة هيمنة رقمية ونمو جماهيري مستدام** تدير خطين متوازيين للإنتاج:
1. **مصنع الفيديو (Shorts & Long Documentaries):** تحقيق معدل إكمال (Retention > 80%) عبر سيكولوجية الغموض والنوستالجيا المظلمة وأسلوب المانيكان ثلاثي الأبعاد الآمن إعلانياً.
2. **مصنع البوسترات والصور اليومية (Daily Brand & Ad Posters):** ظهور بصري يومي ثابت لبراند "شاهر" على (Instagram, Facebook, TikTok) بتصاميم إعلانية جذابة ومحتوى تفاعلي يزيد التحويل والمتابعين.
3. **مساعد المحادثات الشخصي على تليجرام:** واجهة تفاعل ذكية للدردشة، الأوامر الصوتية، وتوليد الصور الفوري، مع تحكم كامل عن بُعد في مصنع المحتوى.
4. **التكلفة الصفرية والاستقلالية:** كل العمليات الثقيلة (ريندر، تفريغ، تنسيق) تعمل على الجهاز دون اشتراكات، مع تكلفة هامشية = 0$.

---

### 2. إدارة العتاد والأحمال (Strict Hardware Allocation — 8GB VRAM)
الجهاز: **ASUS TUF Gaming F15 (i7-13620H / 16GB RAM / RTX 4070 Laptop 8GB VRAM / Win 11)**.
لمنع انهيار الذاكرة (Zero OOM Crashes)، يتم توزيع المهام هندسياً:
- **العقل المدير والناقد المحلي (Ollama):** تشغيل `qwen2.5:7b` على كارت الشاشة (استهلاك ~4.8GB VRAM).
- **محرك تفريغ النصوص (Faster-Whisper):** على أنوية المعالج المركزي (i7 CPU) بنمط `int8` (استهلاك 0GB VRAM، تفريغ في 1.8 ثانية).
- **محرك المونتاج (FFmpeg):** تشفير عتادي فائق عبر شريحة `h264_nvenc` في الكارت (<0.1GB VRAM، ريندر فوري في 1.63 ثانية).
- **المتصفح (Playwright CDP):** اتصال بمتصفح Chrome الحالي المفتوح عبر منفذ Remote Debugging `9222`.

---

### 3. خطوط إنتاج المحتوى الثلاثة (The 3 Production Pipelines)

#### 🎨 أولاً: خط البوسترات والتصاميم اليومية (Daily Visuals & Posters Engine)
- **المصدر والتخطيط:**
  - يتصل المتصفح بـ Gemini عبر الشات المثبت المخصص **("شاهر")**.
  - يستخرج فكرة البوست/الإعلان اليومي، نص الكابشن المقنع، والبرومبت البصري الاحترافي.
- **التوليد البصري:**
  - يفتح موقع **Flow** ويختار نموذج **Nano Banana 2**.
  - يولد التصميم بمقاسات السوشيال ميديا المحسنة (4:5 لإنستغرام وفيسبوك، أو 9:16 لتيك توك Photo Mode) بجودة فائقة وإضاءة تجارية قوية.
  - تحميل التصميم تلقائياً إلى مجلد `workspace/downloads/images/`.
- **النشر المتزامن التلقائي:**
  - **Meta Business Suite:** رفع البوستر ونشره في نفس اللحظة على صفحة **الفيسبوك** وحساب **الإنستغرام** مع الكابشن والهاشتاجات المخصصة وروابط الـ CTA.
  - **TikTok Web Studio:** رفع الصورة كـ (Photo Post) لجذب تفاعل خوارزميات الصور الجديدة على تيك توك.

---

#### ⚡ ثانياً: خط فيديوهات الشورتس الفيروسية (Viral Shorts Engine)
يُمنع اعتماد أي سكريبت شورتس إلا بعد استيفاء القواعد الخمس الصارمة لرفع الـ Retention:
1. **الـ Hook القاتل (0 - 3 ثوانٍ):** صدمة بصرية وصوتية فورية تكسر التوقع وتبث الفضول دون مقدمات أو ترحيب.
2. **سد فجوة الفضول تدريجياً (Progressive Curiosity):** بناء توتر استقصائي تصاعدي؛ لا يُكشف السر إلا في الثلث الأخير.
3. **ريتم التحفيز البصري (2.5s Visual Cut):** انتقال المشهد أو تطبيق تقريب ديناميكي ناعم (Ken-Burns ZoomPan) كل 2.5 ثانية لمنع ملل العين.
4. **الترجمة المغناطيسية (Viral ASS Subtitles):** خط `Segoe UI Black` عريض، لون أصفر نيون (`&H0000FFFF`)، حدود سوداء سميكة (`Outline=5`)، وهامش رأسي آمن (`MarginV=380`) لا تغطيه أزرار المنصات.
5. **النهاية الحلقية والجدل (Loop & Debate Trigger):** كلمة ختامية تتصل بالجملة الأولى تلقائياً، وسؤال يثير خلافاً وتعليقات نارية في الكومنتات.

---

#### 🎬 ثالثاً: خط الفيديوهات الوثائقية الطويلة (3D Mannequin Documentary Engine)
لإنتاج وثائقيات 8 إلى 12 دقيقة بحبكة متماسكة وثبات بصري كامل:
- **نظام المانيكان ثلاثي الأبعاد المفرغ (Faceless 3D Mannequin):**
  - **المانيكان الأبيض:** الشخصيات المحايدة، المارة، الشرطة، والمحيطين.
  - **المانيكان الأحمر:** مصادر الخطر، المعتدون، والتهديد (إشارة بصرية للخطر كبديل آمن عن الدماء لتفادي حظر الإعلانات).
  - **المانيكان الأزرق:** الضحايا، الذكريات، ومشاهد الفلاش باك.
- **المونتاج:** 4 فصول وثائقية تجمع لقطات حركية من Veo Lite، وصور 4K من Nano Banana 2 مع خلفيات صوتية غامضة خفيفة (Dark Ambient).
- **الغلاف الصادم (Split-Screen Thumbnail):** صورة منقسمة بنظام المقارنة (قبل / بعد، ألوان مشبعة) وعنوان عربي ضخم لا يتجاوز 3 كلمات.

---

### 4. هرم المحتوى والجدولة الأسبوعية (Weekly Batch Engine)
عند إعطاء أمر التشغيل الأسبوعي (`Start Weekly Batch`):
- **يومياً:** 1 فيديو شورتس + 1 بوستر/تصميم يومي (فيسبوك، إنستغرام، تيك توك).
- **أسبوعياً:** 2 فيديو وثائقي طويل (الخميس والاثنين) مقسمة لفصول وثائقية.
- **الجدولة الآلية:** فتح استوديوهات النشر وضبط خيار "Schedule" على ساعات الذروة الجماهيرية تلقائياً.

---

### 5. حلقة النقد القاسي والتعلم الذاتي (The Ruthless Critic Loop)
- **Ollama المحلي كناقد:** يراجع سكريبتات الشورتس وكابشن البوسترات قبل النشر؛ إذا نال تقييماً أقل من 8/10 يُرفض ويُعاد تحسينه فوراً.
- **مراجعة 24h و 48h:** سحب إحصائيات التفاعل ونسب الـ Retention من Supabase وتعديل توجيهات الـ Hooks والتصاميم القادمة تلقائياً.

---

### 6. شبكة الأمان والبدائل (Failover Resilience Chain)
- **الصوت (Voiceover):** Google AI Studio TTS Playground ➔ البديل المحلي: `edge-tts` بصوت `ar-EG-ShakirNeural`.
- **الأفكار والبرومبتات:** محادثات جيمناي المثبتة ("ده قناة اليوتيوب" للفيديوهات، و"شاهر" للبوسترات) ➔ البديل: Qwen.ai / DuckDuckGo AI ➔ البديل: Ollama محلياً.
- **التوليد البصري:** Flow (Nano Banana 2 للبوسترات والمشاهد + Veo Lite للتحريك) ➔ البديل: SeekAI / Flux محلياً.

---

### 7. نظام المساعد الذكي والمحادثات على تليجرام (Telegram AI Assistant)
- **محادثات ذكية (نص وصوت):** محاكاة قدرات ChatGPT و Claude في النقاش، البرمجة، والرد الصوتي المباشر.
- **توليد صور فوري (`/imagine`):** توليد وإرسال صور عالية الجودة في الشات فوراً وتوثيقها في Supabase.
- **لوحة قيادة لمصنع المحتوى عن بُعد:**
  - `/content_run` : إطلاق دورة إنتاج شورتس وبوستر اليوم فوراً وإرسال المعاينة لتليجرام.
  - `/content_batch` : تشغيل خطة إنتاج وجدولة الأسبوع بالكامل (فيديوهات + بوسترات).
  - `/documentary` : إنتاج وتصدير حزمة فيلم وثائقي استقصائي كامل بنظام 3D Mannequin.
  - `/content_status` : تقرير بلحظات الإنتاج، نسب الـ Retention، وتفاعل المنشورات.

---

### 8. مخطط قاعدة البيانات في Supabase (Database Schema)
- `content_tasks`: تسجيل دورات الإنتاج، نوع المهمة (`short`, `long_form`, `poster_image`)، السكريبت، ومسارات الملفات.
- `content_posts`: توثيق روابط المنشورات على المنصات الأربع وتواريخ الجدولة.
- `content_analytics`: تسجيل المشاهدات، التفاعل، الـ Retention Rate، وتقييم Ollama الدوري للأداء.
